services:
  jupyter:
    image: cnbusaly/tools-models:latest
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    command: start-notebook.py --IdentityProvider.token=''
    restart: unless-stopped
